/* =========================================================
   FILE: 10_mount.js
   PHẢI LOAD SAU CÙNG — render component App (từ 09_app_router.js) vào #root

   Không cần sửa file này trừ khi đổi cách mount React (VD: thêm StrictMode).
   ========================================================= */
ReactDOM.createRoot(document.getElementById("root")).render(<App />);
