/* =========================================================
   FILE: 00_react_hooks.js
   PHẢI LOAD ĐẦU TIÊN — mọi file khác đều dùng useState/useEffect/useRef

   Chỉ khai báo các React hook dùng chung, không có UI.
   ========================================================= */
const { useState, useEffect, useRef } = React;
